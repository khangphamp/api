const c =10000000;
let array = Array(c)
console.time("for")
for(let i = 0; i <= c; i++){
  // 
}
console.timeEnd("for")
console.time("forEach")

array.forEach(element => {
  // 
});
console.timeEnd("forEach");
